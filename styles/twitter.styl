.twitter {
    background-color: medium-dark
}

.tweets {
    @extend $constrain
    mobile-padding-media()
}

.tweet {
    position: relative;
    font-size: 1rem
}

.tweet:last-child {
    margin-top: 2rem

    @media (max-width: constrained-width) {
        margin-top: 0;
    }
}

.tweet__icon {
    position: absolute;
    width: 2rem
    height: auto

    img {
        border-radius: 2px;
    }
}

.tweet__icon + .tweet__meta,
.tweet__icon + .tweet__meta + .tweet__content,
.tweet__icon + .tweet__meta + .tweet__content + .tweet__retweet {
    margin-left: 4rem
}

.tweet__meta {
    font-weight: bold
}

.tweet__retweet {
    margin-top: 0.25rem
    color: subtitle-gray
    font-size: smaller
}

.tweet__retweet-icon {
    display: inline-block
    margin-right 6px
    vertical-align: middle

    path {
        fill: #959CA3
    }
}

.tweet__viewmore {
    view-more()
}

.twitter__handle {
    display: inline-block
    padding-left: 0.25rem
    font-weight 300
}

a.twitter__handle:link {
    color: #999999;
}

/* visited link */
a.twitter__handle:visited {
    color: #999999;
}

.tweet__content {
    color: subtitle-gray
    margin-top: 0.25rem
}
